[project]
name = "hotel-reservations-api"
version = "1.0.0"
description = "API de Reservas Hoteleras - Proyecto Semana 03"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn[standard]>=0.40.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
